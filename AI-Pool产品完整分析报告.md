# AI Pool — 产品完整分析报告

> 生成日期：2026-05-16
> 基于代码库 + docs/ 文档 + 规划方向深度整理

---

## 一、产品简介

**AI Pool** 是一个**企业级多模型 AI 聚合平台**，以 Go/Gin 后端 + Next.js 前端的架构，提供对 OpenAI（GPT-5.x、GPT Image-2）、Anthropic（Claude 3.5）、Google（Gemini 2.0）、DeepSeek（V4 Pro/R1）、Moonshot（Kimi K2.5/K2.6）等五大主流模型家族的**统一接入与管理**。

它不是又一个 Chat 界面，而是一个**可以完全品牌化、可以定制模型列表、可以自建计费体系、可以嵌入自己产品的 AI 能力中台**。

### 一句话定位

> **「一个平台调用所有大模型，还能定制成你的品牌」**

### 已落地功能全貌

| 模块 | 能力 | 状态 |
|------|------|------|
| 🗣️ 多模型聊天 | 流式对话、多模型切换、历史管理、重命名、置顶、删除 | ✅ 成熟 |
| 🔄 并列对比 | 同时选 2 个模型 → 同一问题 → 分栏对比 | ✅ 成熟 |
| 🖼️ 图片生成 | gpt-image-2、多纵横比(1:1~21:9)、分辨率(1K~4K)、质量选择 | ✅ 成熟 |
| ✂️ 图片编辑 | 背景移除、背景替换、文字移除、画质提升（图生图） | ✅ 成熟 |
| 🎨 技能系统 | 9 种预设角色（CEO 策略师/代码审查/翻译等）+ 用户自定义技能 | ✅ 成熟 |
| 📊 PPT 生成 | AI 生成 PPT 大纲、多风格模板（商务/创意/极简等）、Markdown 导出 | ✅ 成熟 |
| 📎 文件解析 | PDF/Office → 文本提取 → 分块 → Embedding → 向量检索（RAG） | ✅ 成熟 |
| 🔗 对话分享 | 通过 short slug 分享完整对话或对比记录 | ✅ 成熟 |
| 💳 积分体系 | 三级积分（基础/高级/精英）+ 免费额度 + 套餐定价 | ✅ 成熟 |
| 📝 回答模板 | system prompt 预设模板，控制输出风格 | ✅ 成熟 |
| 🔒 认证系统 | JWT + bcrypt + 登录/注册 | ✅ 成熟 |
| 🌙 主题切换 | CSS 变量驱动的日/夜间主题，30+ 变量覆盖全局 UI | ✅ 成熟 |

### 技术架构特色

```
┌─ 前端 ─────────────────────────────────────────────┐
│ Next.js 14 + TailwindCSS + shadcn + static export  │
│ 纯静态产物 → 无 Node 运行时，CDN 友好               │
├─ 后端 ─────────────────────────────────────────────┐
│ Go 单二进制字节码（~9,000 行）                     │
│ Gin 路由 + GORM 持久层 + goroutine 流式处理        │
│ SQLite/PostgreSQL 互换（一套代码兼容）              │
├─ 模型抽象层 ──────────────────────────────────────-┐
│ AIService → Provider 接口                          │
│ SSE 流式管线统一（500 行流式架构重构定稿）          │
│ 错误重试 + 超时控制 + context 取消传播              │
└────────────────────────────────────────────────────┘
```

---

## 二、产品核心优势

### 维度一：深度可定制产品界面（品牌白标能力）

AI Pool 的前端不是写死的一锤子 UI，而是**从 CSS 变量到页面路由到组件配置，每一层都面向定制**。

#### 界面定制化能力分级

| 层级 | 可定制项 | 实现方式 | 耗时 |
|------|---------|---------|------|
| 🏷️ **品牌标识** | Logo、产品名称（当前"AI Space"→任改）、Favicon、页面标题 | 组件层 1-2 处替换 | 10 分钟 |
| 🎨 **品牌色系** | 主色 `--brand`、背景 `--background`、前景 `--foreground`、卡片 `--surface-card`、边框 `--border`、文字全部等级、Accent 色、Muted 色 | `globals.css` + `tailwind.config.ts` CSS 变量体系，30+ 变量全覆盖 | 20 分钟 |
| 🌓 **主题体系** | 深色/浅色双主题独立配色，每套主题可配置全部颜色变量 | `ThemeProvider` 通过 `data-theme` 属性切换 | 预置 |
| 🔧 **页面路由** | 选择暴露或隐藏：聊天 / 图片 / PPT / 定价 / 模板 / 技能市场 / 分享页 | `next.config.js` + 路由组组织 | 按需 |
| 🖼️ **着陆页** | Hero 区、特性展示、模型展示、统计数字、CTA 按钮 —— 全部组件化可替换 | 独立 Section 组件 | 按需 |
| 📱 **侧边栏** | 折叠/展开、快捷操作（新对话/聊天/技能/图片/PPT/更多）、工具分组、icon 映射 | `AppSidebar.tsx` 配置化 | 按需 |
| 💳 **定价页面** | 套餐数量、价格、积分额度、功能列表、按钮文案 | 纯配置文件 | 按需 |
| 🎯 **技能映射** | 每个预设技能的 icon、color、prompt 独立可配 | `SKILL_ICON_MAP` + skill JSON | 按需 |
| 🌐 **域名/SSL** | 自定义域名 + HTTPS 证书 | Nginx 反向代理层 | 30 分钟 |
| ✅ **品牌去标** | 移除任何「Powered by」标识，100% 白标 | 全局配置开关 | 5 分钟 |

#### 商业价值

> **企业不需要从头开发一个 AI 平台。给他们一套 CSS 变量 + 一行 Logo 配置，就能拥有属于自己的 AI 平台。**

适用场景：
- **教育机构**：用自己品牌色 + Logo，给学生提供 AI 学习助手
- **企业内部**：作为公司 AI 中台，员工用企业 SSO 登录
- **咨询公司**：为客户定制品牌 AI 平台，作为增值服务交付
- **SaaS 产品**：嵌入自有产品中，提升产品智能度

---

### 维度二：稳定可靠的技术底座（后端能力承载）

AI Pool 的后端不是简单的 API 转发层，它是一套**完整的 AI 平台基础设施**。

#### 2.1 多模型统一接入层

```
                    ┌─ OpenAI (GPT 5.x / GPT Image 2)
                    ├─ Anthropic (Claude 3.5)
用户请求 → AIService →─ Google (Gemini 2.0 Flash)
                    ├─ DeepSeek (V4 Pro / R1 / V4 Flash)
                    └─ Moonshot / Kimi (K2.5 / K2.6)
```

- 每个模型家族有**独立配置**（Base URL、API Key、超时参数）
- 支持**独立的代理/中转部署**（可走反向代理/中间件隧道）
- 统一 `ChatCompletion()` / `ChatStream()` 接口，新增一个模型家族只需 1 个文件
- 已支持 `web_search` 原生搜索 + 第三方搜索（Tavily + Brave 自动降级）

#### 2.2 企业级流式管线

后端架构文档（`docs/AI对话流式架构-最终定稿.md`）详细描述了投入 **5 轮迭代重构**的统一流式管线：

```
用户请求 → Gin Handler
  ├─ panic recovery（崩溃保护）
  ├─ IP Rate Limiter（限流保护）
  ├─ 请求验证
  ├─ AIService.ChatCompletion()
  │    ├─ 流式：SSE 实时推送到前端
  │    └─ 非流式：一次性返回
  ├─ token 追踪、错误重试、超时控制
  ├─ reasoning 标签 / search 标签 / error 标签
  └─ 自动保存 assistant 消息到 DB
```

#### 2.3 图片引擎（生成 + 编辑）

- **异步架构**：提交后立即返回 `pending` 状态，后台 goroutine 完成，支持服务重启后恢复
- **文生图**：gpt-image-2，多纵横比映射、分辨率控制、质量选择
- **图生图编辑**：背景移除、背景替换、文字移除、画质提升（均基于 GPT Image Edit API）
- **参考图**：通过 `file_publicId` 从已上传文件库引用

#### 2.4 文件 RAG 流水线

从上传到检索的完整链路：

```
上传文件（PDF/Office/图片）
    ↓
FileParser 解析（文本提取、OCR、图片 caption）
    ↓
文本分块 → Embedding 向量化
    ↓
向量检索 → 注入 Chat 上下文
    ↓
模型基于上下文回答
```

- 文件元数据完整记录（token 数、页数、MIME 类型、解析状态）
- Embedding 支持多 provider 共存，`uniqueIndex` 防重复
- 开关控制（`ENABLE_EMBEDDING`），无 Embedding 时自动降级关键词搜索

#### 2.5 技能插件系统

不是简单的 prompt 预设，而是**运行时热加载的插件架构**：

- 技能 manifest：名称、描述、icon、category、co_skills、system_prompt
- REST API 完整：创建、更新、删除、列出、获取
- 支持 `is_meta` 技能组合 `co_skills`，形成复合能力
- 用户可自定义私有技能

#### 2.6 三级积分计费

- 基础/高级/精英三级积分，模型按等级归类
- Token 级自动扣减 + refund 逻辑（失败回滚）
- 免费版每日自动重置 30 基础积分
- 4 档定价套餐（Free/Basic/Plus/Ultra）
- API 端到端完整：查询余额、扣减、分级定价、模型 tier 映射

#### 2.7 Go 技术栈优势

| 特性 | AI Pool（Go） | 竞品 Python/Node |
|------|--------------|-------------------|
| 部署 | 单文件二进制，scp 即部署 | 需要 Python/Node 运行时 |
| 内存 | ~50MB 运行 | Python ~200MB+ |
| 冷启动 | 毫秒级 | 秒级 |
| 并发 | goroutine 天然适合 SSE | 需要异步框架 |
| 跨平台 | `GOOS=xxx go build` 一键 | 依赖平台包 |

---

## 三、稀缺性能力（已具备 + 规划方向）

### ✅ 当前已具备的稀缺能力

| 能力 | 稀缺性价值 | 竞品对标 |
|------|-----------|---------|
| **并列对比（Compare）** | 同时 2 个模型对比输出，搭配回答模板实现公平对比 — 研发选型、Prompt 调试、模型评测的高频刚需 | ChatGPT/Claude.ai 无此功能 |
| **图片编辑工作台（4 种编辑模式）** | 背景移除/替换/文字移除/画质提升，聚合在一个 AI 平台中 | ChatGPT 只有 DALL·E 生成，无编辑工作台 |
| **技能插件动态注入** | 运行时热加载，不是简单的角色选择，而是可编程 agent manifest | Poe 有 bot 但不能热加载 |
| **三级积分 + Token 级计费** | 基础/高级/精英三级 + Token 级别自动扣减 + refund 回滚 | OpenRouter 按固定 token 计价 |
| **SSE 统一流式管线** | 5 轮迭代重构的统一管线，统一错误/超时/重试逻辑，支持 reasoning/search 标签 | OpenRouter 只做代理转发 |
| **完整的文件 RAG 流水线** | 上传→解析→分块→Embedding→检索→注入对话上下文，全自研 | Poe 不支持 RAG，ChatGPT 文件仅限 GPT-4 使用 |
| **单二进制可部署** | Go 原生编译 + 前端静态导出，一个二进制 + 一个 dist 目录 = 部署完毕 | 竞品需 Node/Python 运行时 + 复杂依赖 |

### 🚀 规划中的稀缺能力

根据你的指引和 [ChatGPT 提示链接](https://chatgpt.com/s/t_6a0661370ccc8191969629e8d77f8710)，将能力扩展方向分为三大板块：

---

### 板块一：Workspace → Workspace Agent → 专业化 Agent → 专业化 Agent 模型蒸馏

这是从**工具到智能体的进化路径**，分成 4 个阶段：

#### 阶段 1：Workspace（工作区 1-2 周）

当前 AI Pool 只有对话，没有"工作区"的概念。Workspace 是结构化的项目/任务容器。

| 能力 | 描述 | 技术路径 |
|------|------|---------|
| **项目级工作区** | 对话按项目分组，项目内共享上下文、文件、技能 | 数据模型加 `workspace` 表 + 前端侧边栏切换 |
| **任务看板** | 项目内创建任务，每个任务关联对话 | 轻量看板（类 Trello）界面 |
| **文件库** | 项目级别共享的文件仓库，跨对话复用 | 已有文件系统 + workspace_id 字段 |
| **项目模板** | 预置公司 SOP 模板（市场调研/竞品分析/周报），创建工作区即灌入初始对话 | JSON 配置模板 |

#### 阶段 2：Workspace Agent（工作区智能体 2-3 周）

工作区不仅是一个容器，它有一个**常驻 Agent** 负责观察和辅助。

| 能力 | 描述 | 技术路径 |
|------|------|---------|
| **自动上下文感知** | Agent 理解工作区中所有对话和文件，新对话自动带入相关上下文 | 已有 RAG 流水线 + 工作区级索引 |
| **智能建议** | 根据工作区活跃任务，自动建议下一步操作 | 基于任务状态 + AI 推理 |
| **周期性报告** | 每日/每周自动生成工作区进展摘要 | cron job + AI 总结 |
| **会议纪要自动生成** | 上传录音/文字稿 → 自动生成结构化会议纪要 → 创建任务 | 文件解析 + AI 分析 |

#### 阶段 3：专业化 Agent（3-4 周）

当用户有了工作区和 Agent，下一步是为**特定专业场景**训练/配置 Agent。

| 专业方向 | 能力 | 用户价值 |
|---------|------|---------|
| 📈 **市场研究 Agent** | 联网搜索→多源信息聚合→输出研究报告 | 替代手动百度/Google 拼凑信息 |
| 📝 **内容创作 Agent** | 选题→大纲→初稿→多版迭代→发布 | 自媒体/运营团队提升 5x 效率 |
| 💻 **代码开发 Agent** | 需求→架构→代码→Review→部署 | 开发团队协作编码 |
| 🏭 **企业运营 Agent** | 周报→KPI 追踪→异常预警→自动生成报告 | 管理层数据驱动决策 |
| 🏛️ **法律/合规 Agent** | 合同审查→法规检索→风险提示 | 法务团队效率提升 |

#### 阶段 4：专业化 Agent 模型蒸馏（1-3 个月）

这是最稀缺、壁垒最高的能力。

**概念**：当某个专业化 Agent 长期在特定领域运行（如"市场研究 Agent"处理了 10,000+ 次真实客户查询），我们积累了**大量高质量的领域对话对 + 偏好数据**。用这些数据对通用模型做 LoRA / DPO 蒸馏，得到一个**比 GPT-5.5 更小、更便宜、但在该领域表现更好的专用模型**。

| 阶段 | 输入 | 输出 | 差异化 |
|------|------|------|--------|
| **数据积累** | 10,000+ 条领域对话历史 | 高质量的 prompt-response 对 + 用户偏好（好/差评价） | 独家数据 |
| **蒸馏训练** | 上述数据 → LoRA / DPO 微调 | 专用模型文件（GGUF 格式） | 领域专用 |
| **推理部署** | 部署 vLLM / llama.cpp 端点 | 比 GPT-5.5 便宜 10x 的专用推理 | 成本优势 |
| **持续迭代** | 新数据持续回流 → 定期增量微调 | 模型在领域持续进步 | 飞轮效应 |

**商业价值**：

```
通用模型（GPT-5.5）：$10/百万 Token
蒸馏后专用模型：$1/百万 Token（便宜 10 倍）
专用场景质量：≈ GPT-5.5 或更优
```

*这正是 OpenAI 推出 GPT 系列 fine-tuning API 背后逻辑的本地化落地。*

---

### 板块二：实用工具类插件

将 AI Pool 从「聊天平台」升级为「AI 工具生态」，用户可以通过插件市场安装各种实用工具。

#### 2.1 沉浸式翻译插件（1-2 周）

| 能力 | 技术路径 |
|------|---------|
| **网页翻译** | 浏览器插件形式（或 Service Worker 代理），选中文本→AI 翻译→覆盖/旁注 | 已有翻译技能 + Chrome Extension |
| **PDF 翻译** | 上传 PDF → 逐页翻译 → 保持排版输出 | 已有文件解析 + AI 翻译 |
| **双语对照** | 原文 + 译文并行展示，逐段对照 | 前端分栏渲染 |
| **术语库** | 用户自定义术语翻译规则（专业词汇一致性） | 配置式术语表 |
| **全文 vs 段落** | 即时翻 vs 批量全文翻 | 两种模式可选 |

**对标**：沉浸式翻译（10 万+ 用户 Chrome 插件），但使用自己的 AI 模型池，不限额度、不限厂商。

#### 2.2 网页内容总结插件（1-2 周）

| 能力 | 描述 |
|------|------|
| **URL 一键总结** | 用户发送 URL → 抓取正文 → AI 生成摘要（100 字/500 字/列表式） |
| **多级摘要** | 一句话概括 / 要点列表（3-5 点）/ 详细分析（含观点提取） |
| **对比阅读** | 同时总结 2-3 个 URL，输出对比表格 |
| **后续提问** | 基于总结内容追问细节 |

**实现节点**：前端聊天输入框 + `web_extract` 抓取引擎 + AI 摘要推理。

#### 2.3 更多工具插件（长期规划）

| 插件 | 能力 | 对标 |
|------|------|------|
| **AI 搜索增强** | 用户搜索 → AI 理解意图 → 多源搜索 → 结构化回答 | Perplexity / 秘塔 AI |
| **代码解释器** | 写代码 → 执行 → 看结果 → 迭代修改 | ChatGPT Code Interpreter |
| **数据分析助手** | 上传 CSV → 自动分析 → 图表 → 洞察报告 | Julius AI |
| **长文档深度阅读** | 上传 500 页 PDF → 多轮 Q&A → 知识图谱 | ChatPDF/Kimi |
| **语音笔记转文字** | 录音 → 转写 → 总结 → 结构化 | Otter.ai / 飞书妙记 |
| **图片 OCR 增强** | 截图/扫描件 → OCR → AI 结构化提取 | 百度 OCR |
| **知识库构建器** | 上传多份文档 → 自动建索引 → 专属知识问答 | 语雀 AI / Notion AI |

### 板块三：平台基础设施能力

这些是支撑前面两大板块运转的底层能力。

| 能力 | 优先级 | 说明 |
|------|--------|------|
| **OpenAI API 兼容网关** | P0 | 用户用 OpenAI SDK 直接调 AI Pool，零迁移成本接入现有工具链 |
| **多租户（Org/Workspace 隔离）** | P0 | 一个部署服务多个客户，数据完全隔离 |
| **管理员后台** | P1 | 用户管理、用量统计、模型开关、日志查看、定价配置 |
| **嵌入 Widget** | P1 | 一行 `<script>` 嵌入客户网站 |
| **数据看板** | P1 | API 调用趋势、Token 消耗分布、模型热度、每日活跃 |

---

## 四、定制化能力矩阵（对客户的完整交付清单）

### 对客户的统一销售语言

| 你想做什么 | AI Pool 给你什么 | 怎么交付 |
|-----------|----------------|---------|
| "我要一个完全**属于我品牌**的 AI 平台" | ✅ Logo、名字、色系、域名全定制 | 替换 CSS 变量 + 配置项，1 小时 |
| "我要**哪些模型给我的用户用**" | ✅ 模型列表可开关、可排序、可定价 | `.env` 配置，5 分钟 |
| "我要**给我的用户计费**" | ✅ 三级积分 + Token 级计费 + 套餐配置 | 配置后台，开箱即用 |
| "我要**给我的团队不同角色**" | ✅ 技能插件系统，12+ 预设 + 自定义 | API 创建无需重启 |
| "我要**用户上传文件让 AI 分析**" | ✅ PDF/Office → RAG → 问答 | 开箱即用 |
| "我要**生成和编辑图片**" | ✅ 文生图 + 4 种编辑模式 | 开箱即用 |
| "我要**分享对话给客户看**" | ✅ URL 分享 + 对比记录分享 | 开箱即用 |
| "我要**嵌入到我自己的网站**" | 🚧 嵌入 Widget（规划中） | 一行 `<script>` |
| "我要**多个团队互相隔离**" | 🚧 多租户 Org（规划中） | 后台配置 |
| "我要**我的用户也能调 API**" | 🚧 API Key 自服务（规划中） | 同 OpenAI 协议 |

### 竞品对比一览

| 维度 | AI Pool | ChatGPT | Poe | OpenRouter |
|------|---------|---------|-----|------------|
| **多模型聚合** | ✅ 5 大家族 | ❌ 仅 OpenAI | ✅ | ✅ 200+ |
| **品牌白标** | ✅ 完整能力 | ❌ 不可定制 | ❌ | ❌ |
| **并列对比** | ✅ 核心功能 | ❌ | ❌ | ❌ |
| **图片编辑** | ✅ 4 种编辑 | ❌ 仅生成 | ❌ | ❌ |
| **文件 RAG** | ✅ 全自研流水线 | ✅ GPT-4 可用 | ❌ 有限 | ❌ |
| **技能插件** | ✅ 热加载 | ❌ GPTs 不开源 | ✅ Bots | ❌ |
| **本地部署** | ✅ 单二进制 | ❌ | ❌ | ❌ |
| **三级积分** | ✅ Token 级别 | ❌ 固定订阅 | ✅ 点数 | ✅ 按 Token |
| **对话分享** | ✅ URL Slug | ✅ | ❌ | ❌ |

---

## 五、差异化竞争定位

### 我们的位置

```
                   能力深度
                      ↑
            AI Pool ● │ ● 企业定制
          （白标+聚合+增强） │
                      │
         Poe ●        │        ● ChatGPT
     （多模型市场）    │      （品牌+生态）
                      │
                      │        ● OpenRouter
                      │   （纯 API 代理）
                      └─────────────────────→ 品牌/生态广度
```

**我们不跟 ChatGPT 比品牌，不跟 OpenRouter 比模型数量。**

我们的核心壁垒是：

1. **「开箱即用的白标 AI 平台」** — 从界面到能力，从计费到分享，一个二进制搞定
2. **「聚合 + 增强」** — 不仅仅是路由转发，还提供 RAG、图片编辑、技能插件等**开放平台不具备的上层能力**
3. **「单机可部署」** — Go 二进制 + 静态前端，低资源、低运维
4. **「规划中的专业 Agent 生态」** — 从对话工具 → 工作区 → 专业化 Agent → 模型蒸馏，逐级递进的护城河

### 核心公式

> **多模型聚合 × 品牌白标 × 能力增强 × Agent 生态 = 不可替代的 AI 中台**

---

## 六、总结与行动建议

### 一句话卖点

> **「一个平台调用所有大模型，还能定制成你的品牌。未来还能蒸馏出只属于你的专业 AI。」**

### 建议优先推进（按用户场景）

| 场景 | 最短路径 |
|------|---------|
| **有客户要买白标方案** | → 替换 Logo + CSS 变量 + 模型列表，今天就能交付 |
| **有客户要嵌入自己网站** | → 开发 Widget 嵌入组件（1 周） |
| **有客户要 API 调用** | → OpenAI API 兼容网关（2 周） |
| **有客户要内部多团队** | → 多租户 Org + 管理员后台（2-3 周） |
| **我们要建护城河** | → Workspace → Agent → 蒸馏（持续投入） |

---

## 附录：核心文件索引

| 文件 | 内容 |
|------|------|
| `docs/产品分析-AI-Pool-AI聚合平台深度分析.md` | 早期版本的产品深度分析（覆盖白标+后端+稀缺能力） |
| `docs/AI聚合平台策划案.md` | 项目定位、竞品分析、功能模块拆解、商业模式 |
| `docs/AI对话流式架构-最终定稿.md` | 流式管线技术架构（第 5 轮定稿） |
| `docs/文件上传解析与Embedding架构.md` | 文件 RAG 完整架构文档 |
| `后端/internal/api/router.go` | 所有 API 路由定义（198 行） |
| `后端/internal/api/chat.go` | 聊天流式核心实现（~38,000 字节） |
| `后端/internal/api/image.go` | 图片生成/编辑核心实现（18,739 字节） |
| `后端/internal/services/ai_service.go` | 多模型统一服务层 |
| `前端/app/globals.css` | CSS 变量主题系统（15,819 字节） |
| `前端/app/(main)/templates/page.tsx` | 回答模板页面（11,607 字节） |
| `前端/app/(main)/ppt/page.tsx` | PPT 生成页面（16,285 字节） |
| `前端/components/chat/ChatInterface.tsx` | 侧边栏 + 对话管理（34,073 字节） |
