# 账户认证额度用量架构

## 一、定位

账户、额度和用量架构是商业化基础。它同时服务匿名用户试用、登录用户持久化、模型成本控制、套餐展示和接口限流。

## 二、认证层

```mermaid
flowchart LR
  FE[前端 HttpOnly Cookie session + user 快照] --> API[/api]
  API --> Optional[OptionalSessionAuthMiddleware]
  API --> Auth[SessionAuthMiddleware]
  Optional --> Guest[匿名 guest_id]
  Optional --> User[登录 user_id]
  Auth --> User
```

路由分三类：

| 类型 | 示例 | 说明 |
|---|---|---|
| 公开 | `/health`, `/api/models`, `/api/auth/login` | 不需要身份 |
| 可选认证 | `/api/chat`, `/api/files/upload` | 优先从 `ai_space_session` Cookie 识别登录用户；无有效 session 时按匿名 guest 处理 |
| 强认证 | `/api/conversations`, `/api/images`, `/api/videos`, `/api/ppt` | 必须登录 |

当前 Web 登录态规则：

- 浏览器只持有 `ai_space_session` HttpOnly Cookie；前端 JS 不保存、不读取 `localStorage.token` 或 `admin_token`。
- `/api/auth/login`、`/api/auth/register`、`/api/auth/session`、`/api/auth/refresh` 只返回 `user`，不返回浏览器可读 access token。
- 强认证接口由 `SessionAuthMiddleware` 从 Cookie session 解析 `userID`；过渡期保留 legacy `Authorization: Bearer` 兼容非浏览器/旧调用。
- 可选认证接口由 `OptionalSessionAuthMiddleware` 优先解析 Cookie session，否则继续匿名 guest 流程。
- 管理后台复用同一 Cookie session，后台接口额外由后端校验 `role=admin`。

## 三、额度与套餐

后端配置包含：

- 匿名每日聊天次数/金额限制。
- 各 Provider 输入/输出价格。
- 图片、视频按张/次计费。
- 模型 tier 与公开 plans。

前端通过 `CreditBar`、定价页和设置页展示用户额度。

## 四、用量记录

`UsageService` 负责记录 API 调用成本，核心数据进入 `api_usage_logs`。它应记录：

- user/guest
- service/provider/model
- input/output/total tokens
- 单价和总成本
- request/task/message 关联
- success/failure 状态

## 五、限流

`RateLimitMiddleware` 提供 IP 维度保护，并对高频读取端点做豁免，例如图片文件访问、收藏检查、会话列表等。

设计目标：

- 防止匿名用户刷接口。
- 不让历史列表/图片加载这类高频读被误伤。
- 为未来用户级、模型级、Provider 级限流留边界。

## 六、商业化扩展方向

| 能力 | 当前基础 | 后续建议 |
|---|---|---|
| 匿名试用 | guest id + daily limit | 增加设备/IP/成本综合风控 |
| 套餐 | plans endpoint | 接入支付和订阅状态 |
| 成本 | APIUsageLog | 后台成本报表和异常预警 |
| 模型 tier | `/api/models/tiers` | 前端明确展示成本/速度/能力标签 |

## 七、风险点

| 风险 | 影响 | 建议 |
|---|---|---|
| 前端只展示次数不展示成本 | 用户不理解额度消耗 | 模型选择器显示价格/档位 |
| 匿名限制只按次数 | 高成本模型可被滥用 | 优先按金额限制 |
| 用量记录和任务状态脱节 | 成本统计不准 | 任务完成/失败时统一落 usage |
