# 聊天性能与状态稳定改造方案

> 适用项目：AI Space / AI-Pool WebUI  
> 目标范围：`frontend` 聊天主链路、流式输出、消息渲染、消息运行态、任务恢复/轮询边界  
> 当前结论：项目已经从“外部 streaming store 雏形”推进到“结构化 streaming runtime + 平滑流式展示 + reasoning/answer 分离 + 部分消息渲染拆分”的阶段。后续优化应从底层机制转向“Markdown 重型渲染懒加载 + 消息列表边界继续拆分 + 状态/滚动/输入体验打磨”，仍然不建议推倒重写。

---

## 1. 背景与判断

当前聊天体验相关的主要问题集中在两类：

1. **性能问题**
   - 长对话、长 Markdown、长代码块、ECharts、数学公式等内容容易导致渲染压力过大。
   - 流式阶段如果频繁触发 React 主消息状态更新，会造成 `MessageList`、输入框、侧边栏等不必要重渲染。
   - 当前 `frontend/components/chat/MessageList.tsx` 体量较大，职责过多，后续维护和性能定位成本高。
   - 当前 `frontend/components/chat/MarkdownRenderer.tsx` 直接集成 `react-markdown`、`remark-math`、`rehype-katex`、`react-syntax-highlighter`、`EChartsBlock`，属于高成本渲染点。

2. **状态稳定问题**
   - 搜索、生成、思考、后台任务、错误、停止、完成等状态来源较多。
   - 流式 task stream、DB polling、最终消息写回之间如果缺少统一收敛规则，容易出现旧数据覆盖 live 内容、完成后仍显示生成中、报错后状态残留等问题。
   - `frontend/hooks/useChat.ts` 体量较大，承担发送、SSE/task stream、polling、分页、compare、恢复等职责，长期继续堆逻辑风险较高。

调研对照了另一个项目 `AMC-WebUI` 的聊天实现，其核心模式验证了本项目改造方向是正确的：

- 高频 token 不直接进入 React 主消息状态，而进入外部 streaming store。
- 每条消息组件单独订阅自己的流式数据。
- 用 `requestAnimationFrame` 合并通知，避免每 token 触发大量 React 更新。
- 完成时再将最终内容和元数据写入持久消息状态。
- Markdown、代码块、图表、数学渲染需要 lazy / memo / deferred。
- 发送、stream handler、lifecycle、finalizer、message reducer 应有明确边界。

### 1.1 当前已实现状态

截至当前代码状态，文档中部分“待实现”项已经落地或部分落地：

1. **streaming runtime 已从雏形升级为结构化 store**
   - `frontend/lib/streaming.ts` 已存在 `RuntimePhase`、`answerContent`、`reasoningContent`、`realtimeAppendContent`、`realtimeAppendReasoning`、`realtimeSweepExpiredEntries` 等能力。
   - 旧 `stream*` 兼容 API 已完成迁移/清理，主路径已使用结构化 `realtime*` API。
   - reasoning 与 answer 的实时内容已经分离，避免每帧从完整 `<think>` 文本中重复解析。

2. **流式展示已经进入“平滑 + 富文本”阶段**
   - `frontend/hooks/useSmoothStreaming.ts` 已实现 RAF 驱动、60ms 左右节流、后台 tab bypass、结束后继续平滑追赶，避免 DONE 时一帧跳全量。
   - `frontend/components/chat/StreamingText.tsx` 已接入 `answerContent/reasoningContent`，并对正文和 reasoning 分别做平滑输出。
   - 活跃流式正文和 reasoning 已通过 `DeferredMarkdownRenderer` 渲染基础 Markdown，不再是完成前纯文本、完成后才富文本。

3. **reasoning / thinking 渲染已进入组件化阶段**
   - `frontend/components/chat/ThinkBlock.tsx` 已存在，完成态 reasoning 走 Markdown 渲染。
   - 流式 reasoning 与完成态 reasoning 都支持基础富文本，例如加粗、列表、行内代码。

4. **消息列表拆分已经开始，但还没完成**
   - `MessageList.tsx` 中已经出现 `MessageRow` 等拆分边界。
   - 但文件体量仍然较大，`AssistantMessageBubble`、`UserMessageBubble`、`MessageActions`、sources/files/error/compare 等边界仍需要继续抽离。

5. **消息状态派生已部分落地**
   - `frontend/lib/messageStatus.ts` 与 `deriveMessageStatuses` 已存在。
   - 后续重点不是“新增文件”，而是把所有运行态徽标、搜索状态、停止/失败/完成状态逐步统一接入这一派生函数，并补齐浏览器回归。

6. **真实 E2E 与浏览器 fixture 已成为验收基础**
   - 已有真实 `/api/chat` SSE、task status、历史恢复、reasoning 持久化、search/native/fallback 等回归路径。
   - 后续任何聊天体验优化都应继续保留“真实 E2E + 浏览器 fixture + build/nginx smoke”的验收标准。

---

## 2. 总体原则

### 2.1 状态分层

```text
messages state：低频、持久、最终态
streaming.ts：高频、临时、运行态
```

`messages state` 负责：

- 历史消息列表
- 已完成 assistant content
- server message id
- conversation id
- persisted metadata
- usage / credits / completedAt / stopped / error 等最终态

`streaming.ts` 负责：

- 流式 content delta
- reasoning / thinking delta
- 搜索中、文件检索中、生成中、收尾中等运行态
- 当前 task id / background task id / request id
- 当前错误临时态
- 当前 sources / search count 临时态

### 2.2 高频更新不进主消息列表

流式阶段：

```text
后端 token/event
↓
realtimeAppend / realtimeUpdate
↓
当前 assistant message 组件订阅并刷新
```

不要每个 token 都：

```ts
setMessages(prev => prev.map(...))
```

### 2.3 完成时统一 finalization

完成阶段：

```text
SSE/task stream completed
↓
必要时等待 DB/task final state
↓
finalizeAssistantMessage
↓
最终 content/reasoning/status 写回 messages state
↓
realtimeClear(messageId)
```

### 2.4 流式阶段降级渲染

流式阶段不应完整执行高成本渲染：

- 代码高亮
- ECharts 初始化
- KaTeX / MathJax 类数学渲染
- Mermaid / Graphviz 图形渲染
- 大表格完整布局

第一阶段建议：

```text
streaming：纯文本或轻量 Markdown
completed：完整 Markdown / CodeBlock / ECharts / Math
```

后续可做进阶模式：流式阶段支持基础 Markdown，但对敏感内容自动 bypass。

---

## 3. 已确认需要优化的 top 项

### 3.1 `frontend/lib/streaming.ts`：已完成第一轮结构化改造，后续转向硬化

当前已经具备：

- `RuntimePhase` 运行态类型。
- `answerContent` / `reasoningContent` 分离字段。
- `realtimeAppendContent` / `realtimeAppendReasoning` 等结构化写入 API。
- `realtimeSweepExpiredEntries` TTL 清理能力。
- 旧 `stream*` API 的迁移/清理已经完成，主链路不再依赖旧兼容层。
- `useMessageRealtime` / `StreamingText` 已消费结构化 realtime snapshot。

这一项不再是“新增能力”的首要任务，而是进入硬化阶段：

1. **补齐 runtime store 回归矩阵**
   - TTL 后无 listener entry 可回收。
   - completed/stopped/failed 后短 TTL 清理。
   - active listener 不被误删。
   - 大量连续消息不会造成 Map 持续增长。

2. **统一 phase 写入规则**
   - starting / thinking / searching / generating / finalizing / completed / stopped / failed 的写入点要集中管理。
   - terminal phase 必须清理 searchStatus、activityStatus、error 临时态等互斥字段。

3. **减少 legacy `<think>` 解析依赖**
   - 新路径优先消费 `answerContent` / `reasoningContent`。
   - legacy `<think>` 只保留给历史消息、兼容导出和未迁移 provider。

4. **增加调试可观测性**
   - 开发环境可增加 snapshot version / updatedAt / phase 日志开关。
   - 便于定位“旧 polling 覆盖 live 内容”“完成后仍显示搜索中”等问题。

建议新增/保留 API：

```ts
export function realtimeAppendContent(id: string, text: string): void;
export function realtimeSetContent(id: string, content: string): void;
export function realtimeAppendReasoning(id: string, text: string): void;
export function realtimeSetReasoning(id: string, reasoning: string): void;
export function realtimeSetPhase(id: string, phase: RuntimePhase): void;
export function realtimeSweepExpiredEntries(now?: number): void;
```

验收重点从“是否能流式输出”升级为：

- runtime store 不泄漏。
- phase/status 不残留。
- reasoning/answer 不互相污染。
- 历史恢复后不依赖每帧字符串解析。


### 3.2 Markdown 渲染：已支持流式基础富文本，下一步做 AMC 风格懒加载拆分

当前文件：

```text
frontend/components/chat/MarkdownRenderer.tsx
```

问题：

- 同步加载/执行过多重型能力。
- 流式阶段如果反复走完整渲染，会产生明显压力。
- CodeBlock、EChartsBlock 与 MarkdownRenderer 绑定过紧。

目标结构：

```text
frontend/components/chat/markdown/
├── MarkdownRenderer.tsx
├── LazyMarkdownRenderer.tsx
├── BasicMarkdownRenderer.tsx
├── MathMarkdownRenderer.tsx
├── CodeBlock.tsx
├── LazyEChartsBlock.tsx
└── markdownComponents.tsx
```

当前已实现：

- 流式正文通过 `DeferredMarkdownRenderer` 支持基础 Markdown。
- 流式 reasoning 与完成态 reasoning 已走 Markdown 渲染。
- `keepRenderedOnContentChange` 避免高频 token 下 raw fallback 闪回。

下一阶段实现：

1. `MarkdownRenderer` 使用 `React.memo`。
2. 拆出 `CodeBlock`。
3. 长代码块折叠。
4. ECharts lazy load。
5. 非数学内容不加载 math renderer。
6. 流式阶段可选择轻量模式。

长代码建议阈值：

```ts
const LONG_CODE_CHAR_THRESHOLD = 4000;
const LONG_CODE_LINE_THRESHOLD = 120;
```

展示策略：

```text
代码块较长，已折叠 · 138 行
[展开] [复制]
```

ECharts 策略：

- streaming 阶段不初始化。
- completed 后再 lazy render。
- JSON parse 失败时显示代码块，不阻塞整条消息。

---

### 3.3 平滑流式输出 hook：已实现，下一步做体验调参和敏感内容策略

参考 AMC-WebUI 的 `useSmoothStreaming` 思路，当前已经新增：

```text
frontend/hooks/useSmoothStreaming.ts
```

当前已实现：

- 控制 UI 展示节奏，避免每个 token 都 setState。
- 后台 tab bypass animation。
- 流结束后继续平滑追赶，避免最终内容一帧跳全量。
- reasoning 与 answer 分开平滑输出。

下一阶段目标：

- 对超长 lag、移动端低性能设备调参。
- 对未闭合代码块、表格、ECharts、Mermaid、HTML artifact 做更明确的 bypass / deferred 策略。
- 增加真实浏览器性能指标：输入框不失焦、滚动不被拉回底部、模型选择器不重绘。

建议参数：

```ts
const RENDER_THROTTLE_MS = 60;
const DEFAULT_CHARS_PER_FRAME = 1;
```

敏感内容：

- 未闭合代码块
- Markdown 表格
- HTML artifact
- ECharts fence
- Mermaid/Graphviz fence

第一版可以先实现：

```ts
export function useSmoothStreaming(text: string | undefined | null, isStreaming: boolean): string;
```

---

### 3.4 拆分 `MessageList.tsx`：已开始，仍是下一阶段重点

当前文件：

```text
frontend/components/chat/MessageList.tsx
```

目标仍然是将其从“所有消息 UI 的大文件”收敛为列表容器。当前已经出现 `MessageRow` 等边界，但 `MessageList.tsx` 仍然体量较大，下一阶段应继续拆分 assistant/user/action/status/source/file/compare 子组件。

建议结构：

```text
frontend/components/chat/message/
├── MessageVirtualList.tsx
├── MessageRow.tsx
├── UserMessageBubble.tsx
├── AssistantMessageBubble.tsx
├── MessageText.tsx
├── StreamingAnswer.tsx
├── PlainStreamingText.tsx
├── ReasoningBlock.tsx
├── MessageActions.tsx
├── MessageSources.tsx
├── MessageFiles.tsx
├── MessageErrorBlock.tsx
├── CompareMessageSwitcher.tsx
├── types.ts
└── utils.ts
```

关键原则：

1. `MessageList.tsx` 只负责列表、虚拟滚动、加载更多、空状态。
2. `MessageRow` 使用 `React.memo`。
3. `AssistantMessageBubble` 内部调用 `useMessageRealtime(message.id)`。
4. 高频 realtime 更新只影响当前 assistant bubble。
5. 不向 `MessageRow` 传入大型不稳定对象，如完整 models array、大 Map、临时匿名函数。

示例：

```tsx
export const MessageRow = memo(function MessageRow(props: MessageRowProps) {
  if (props.message.role === "user") {
    return <UserMessageBubble {...props} />;
  }

  if (props.message.role === "assistant") {
    return <AssistantMessageBubble {...props} />;
  }

  return null;
}, areMessageRowEqual);
```

比较函数应尽量基于 primitive / stable props：

```ts
function areMessageRowEqual(prev: MessageRowProps, next: MessageRowProps) {
  return (
    prev.message.id === next.message.id &&
    prev.message.content === next.message.content &&
    prev.message.completedAt === next.message.completedAt &&
    prev.message.stopped === next.message.stopped &&
    prev.message.errorCode === next.message.errorCode &&
    prev.isStreaming === next.isStreaming &&
    prev.isSelected === next.isSelected &&
    prev.groupActiveIndex === next.groupActiveIndex
  );
}
```

---

### 3.5 消息状态派生函数：文件已存在，下一步统一接入所有状态展示

当前状态来源仍有分散问题，但基础文件已经存在：

```text
frontend/lib/messageStatus.ts
```

下一阶段目标：

- 将 `message + realtime + isStreaming` 归一成 UI 可直接渲染的状态列表。
- `AssistantMessageMeta.tsx` 只负责展示，不负责复杂判断。
- 分享页、历史页、对比页后续可复用同一状态派生逻辑。
- terminal phase 统一清理搜索中、生成中、工具调用中等临时状态，避免完成后仍显示 running。

建议类型：

```ts
export type MessageDisplayStatus = {
  key: string;
  kind:
    | "generating"
    | "thinking"
    | "web_search"
    | "file_search"
    | "finalizing"
    | "completed"
    | "stopped"
    | "error";
  phase: "running" | "completed" | "failed" | "stopped";
  label: string;
  active: boolean;
  tone: "blue" | "amber" | "green" | "red" | "neutral";
  count?: number;
  retryable?: boolean;
  requestId?: string;
};
```

核心函数：

```ts
export function deriveMessageStatuses(input: {
  message: Message;
  realtime?: RealtimeData;
  isStreaming: boolean;
  t?: (key: string) => string;
}): MessageDisplayStatus[];
```

优先级：

```text
error > stopped > running activity/search > finalizing > completed
```

---

### 3.6 拆分 `useChat.ts` 的 stream / polling / finalizer

当前文件：

```text
frontend/hooks/useChat.ts
```

不建议第一阶段直接大改，但最终应拆分。

建议结构：

```text
frontend/features/chat-streaming/
├── taskEventParser.ts
├── taskEventReducer.ts
├── finalizeAssistantMessage.ts
├── shouldApplyPolledContent.ts
└── types.ts

frontend/hooks/chat/
├── useConversationLoader.ts
├── useChatSender.ts
├── useTaskStream.ts
├── useBackgroundPolling.ts
├── useChatMessagesReducer.ts
├── normalizeMessage.ts
└── types.ts
```

关键函数：

```ts
export function shouldApplyPolledContent(input: {
  streamActive: boolean;
  liveContent: string;
  dbContent: string;
  taskStatus?: string;
}): boolean {
  if (input.streamActive) return false;
  if (!input.dbContent.trim()) return false;
  if (input.dbContent.length < input.liveContent.length) return false;
  return input.taskStatus === "completed";
}
```

目的：

- DB polling 不覆盖正在流式显示的内容。
- completed/finalizing/error/stopped 统一处理。
- task stream 与 polling 的时序由明确函数裁决。

---

## 4. 分阶段实施计划

### PR 1：streaming runtime 第一轮已完成，后续作为硬化任务推进

涉及文件：

```text
frontend/lib/streaming.ts
frontend/hooks/useMessageRealtime.ts
frontend/components/chat/StreamingText.tsx
```

已完成：

- `RuntimePhase`。
- `answerContent` / `reasoningContent` 分离。
- TTL / sweep 能力。
- `realtimeAppendContent`、`realtimeAppendReasoning`、`realtimeSetPhase` 等结构化 API。
- 主链路迁移到结构化 `realtime*` API。
- 流式正文/reasoning 消费结构化 snapshot。

后续任务：

- 补 runtime cleanup/listener/TTL/max-entry 回归。
- terminal phase 统一清理 transient metadata。
- 开发环境增加 snapshot version / phase 调试能力。

验收：

- 普通流式输出正常。
- 连续生成后 runtime store 不持续增长。
- 异常中断后无 listener 的 entry 可被 TTL 回收。
- completed/stopped/failed 后不残留 searching/generating 状态。

---

### PR 2：MarkdownRenderer 拆分与懒渲染，当前最优先

涉及文件：

```text
frontend/components/chat/MarkdownRenderer.tsx
frontend/components/chat/DeferredMarkdownRenderer.tsx
frontend/components/chat/markdown/*
```

已完成：

- 流式正文基础 Markdown 渲染。
- 流式/完成态 reasoning 基础 Markdown 渲染。
- deferred Markdown 在高频 token 下避免 raw fallback 闪回。

下一步任务：

- 拆 `BasicMarkdownRenderer` / `MathMarkdownRenderer`。
- 仅检测到数学语法时加载 math renderer。
- 拆 `CodeBlock`。
- 长代码块折叠。
- ECharts / Mermaid / Graphviz lazy，并默认不在 streaming 阶段初始化。
- 增加浏览器 fixture：长代码、数学、ECharts、nested pre 防回归。

验收：

- 长代码块不明显卡顿。
- ECharts 不在 streaming 阶段初始化。
- 非数学内容不加载数学 renderer。
- Markdown 完成态渲染结果与旧版一致。
- active streaming 的基础 Markdown 不闪回 raw 文本。

---

### PR 3：平滑流式输出第一轮已完成，后续做体验调参

涉及文件：

```text
frontend/hooks/useSmoothStreaming.ts
frontend/components/chat/StreamingText.tsx
```

已完成：

- 60ms 左右 throttle。
- hidden tab bypass。
- DONE 后继续平滑追赶，避免最终内容一帧跳全量。
- reasoning / answer 分开平滑输出。
- active streaming Markdown 与 smooth hook 联动。

后续任务：

- 对超长 lag、低性能设备、移动端调整追赶曲线。
- 对表格、未闭合代码块、HTML artifact、ECharts/Mermaid fence 做敏感内容策略。
- 增加 browser fixture 验证输入框不失焦、滚动不被拉回底部、模型选择器不重绘。

验收：

- 长回答流式更平滑。
- 输入框不失焦、不抖动。
- 表格/代码块流式阶段不明显结构抖动。
- GPT/OpenAI 系模型不会在完成瞬间“一下全出”。

---

### PR 4：拆 MessageList 渲染边界，已开始但仍是重点

涉及文件：

```text
frontend/components/chat/MessageList.tsx
frontend/components/chat/message/*
```

已完成：

- 已出现 `MessageRow` 等初步边界。
- `StreamingText`、`ThinkBlock` 已从部分消息渲染逻辑中抽离。

下一步任务：

- 抽 `AssistantMessageBubble`。
- 抽 `UserMessageBubble`。
- 抽 `MessageActions` / selection bar。
- 抽 `ReasoningBlock`，统一完成态和流式态样式。
- 抽 `MessageSources` / `MessageFiles` / `MessageErrorBlock`。
- compare 分支后移，先稳定普通 row。
- `MessageRow` memo 比较函数收敛到 primitive/stable props。

验收：

- token 更新只重渲染当前 assistant bubble。
- 200+ 消息滚动不卡。
- compare 模式不回归。
- 分享/收藏/删除/复制菜单不回归。

---

### PR 5：消息状态派生函数已存在，下一步统一接入

涉及文件：

```text
frontend/lib/messageStatus.ts
frontend/components/chat/AssistantMessageMeta.tsx
frontend/components/chat/message/*
```

已完成：

- `deriveMessageStatuses` 基础文件已存在。

下一步任务：

- 将 `AssistantMessageMeta.tsx` 中复杂判断迁出。
- search / reasoning / generating / finalizing / completed / stopped / failed 的展示统一走 `deriveMessageStatuses`。
- terminal 状态清理 transient metadata。
- 补搜索完成缺失、DONE 先到、polling 旧内容返回等边界 fixture。

验收：

- 搜索中 → 搜索完成 → 生成中 → 完成顺序稳定。
- 报错后不残留生成中。
- 停止后不显示 running。
- 刷新后 completed 状态正确。

---

### PR 6：拆 `useChat.ts` task stream / polling / finalizer，保持最后推进

涉及文件：

```text
frontend/hooks/useChat.ts
frontend/features/chat-streaming/*
frontend/hooks/chat/*
```

任务：

- 提取 task event parsing。
- 提取 task stream handler。
- 提取 background polling。
- 新增/收敛 `finalizeAssistantMessage`。
- 新增/收敛 `shouldApplyPolledContent`。
- 逐步引入 messages reducer。

验收：

- task stream 与 polling 不互相覆盖。
- DONE 后 DB 延迟时 UI 不闪空。
- 切会话/刷新后任务恢复正确。
- 停止/失败/完成路径都能清理 realtime。


## 5. 关键实现细节

### 5.1 `streaming.ts` 当前实现与后续硬化参考

```ts
const store = new Map<string, RealtimeData>();
const subs = new Map<string, Set<() => void>>();
const touchedAt = new Map<string, number>();
const pending = new Set<string>();
let frame: ReturnType<typeof requestAnimationFrame> | null = null;

const REALTIME_ENTRY_TTL_MS = 5 * 60_000; // 实际值以当前代码为准，可按 active/terminal phase 继续细分
```

统一更新函数：

```ts
function updateEntry(id: string, updater: (prev: RealtimeData) => RealtimeData) {
  realtimeSweepExpiredEntries();

  const prev = store.get(id) ?? { content: "", answerContent: "", reasoningContent: "" };
  const next = updater(prev);

  store.set(id, next);
  touchedAt.set(id, Date.now());
  schedule(id);
}
```

内容更新：

```ts
export function realtimeAppendContent(id: string, text: string) {
  if (!text) return;
  updateEntry(id, (prev) => ({
    ...prev,
    content: prev.content + text,
    answerContent: (prev.answerContent || "") + text,
  }));
}

export function realtimeAppendReasoning(id: string, text: string) {
  if (!text) return;
  updateEntry(id, (prev) => ({
    ...prev,
    reasoningContent: (prev.reasoningContent || "") + text,
  }));
}

export function realtimeUpdate(id: string, patch: Partial<RealtimeData>) {
  updateEntry(id, (prev) => ({
    ...prev,
    ...patch,
  }));
}
```

TTL sweep：

```ts
export function realtimeSweepExpiredEntries(now = Date.now()) {
  for (const [id, lastTouched] of touchedAt.entries()) {
    const listeners = subs.get(id);
    const hasActiveListeners = !!listeners && listeners.size > 0;

    if (now - lastTouched <= REALTIME_ENTRY_TTL_MS) continue;
    if (hasActiveListeners) continue;

    store.delete(id);
    touchedAt.delete(id);
    pending.delete(id);

    if (listeners?.size === 0) {
      subs.delete(id);
    }
  }
}
```

clear：

```ts
export function realtimeClear(id: string) {
  store.delete(id);
  touchedAt.delete(id);
  pending.delete(id);

  const listeners = subs.get(id);
  if (listeners?.size === 0) {
    subs.delete(id);
  }

  if (pending.size === 0 && frame !== null) {
    cancelAnimationFrame(frame);
    frame = null;
  }
}
```

### 5.2 流式渲染选择

```tsx
const realtime = useMessageRealtime(isStreaming ? message.id : "");

const displayContent = isStreaming
  ? realtime?.content || message.content
  : message.content;

const displayReasoning = isStreaming
  ? realtime?.reasoning || message.reasoning
  : message.reasoning;
```

### 5.3 finalization 规则

完成时统一处理：

```ts
finalizeAssistantMessage({
  message,
  realtime,
  dbMessage,
  taskStatus,
  usage,
  error,
});
```

内容选择优先级建议：

```text
1. completed DB content，且长度 >= live content
2. live realtime content
3. existing message content
```

错误优先级：

```text
explicit error > stopped > completed > finalizing > generating
```

---

## 6. 验收标准

### 6.1 性能验收

- 200+ 历史消息打开不卡顿。
- 500+ 消息滚动和向上加载不明显掉帧。
- 长回答流式输出时：
  - 输入框不失焦。
  - sidebar 不闪。
  - 模型选择器不重绘。
- 5000+ 字 Markdown 完成后渲染可接受。
- 300 行代码块不会卡死页面。
- ECharts 不在流式阶段初始化。

### 6.2 状态验收

- 搜索中 → 搜索完成 → 生成中 → 完成，状态顺序稳定。
- 报错后不残留“生成中/搜索中”。
- 用户停止生成后不再显示 running。
- 刷新页面后 completed 状态正确。
- 切会话回来后后台任务能恢复或显示最终状态。
- polling 不覆盖正在流式显示的内容。
- DONE 后如果 DB 内容延迟，UI 显示“正在收尾”而不是突然空掉。

### 6.3 内存验收

- 连续生成多轮后 runtime store 不持续增长。
- 异常中断的 stream 过 TTL 后可回收。
- listener 不大量残留。

---

## 7. 必测场景

1. 普通聊天流式输出。
2. reasoning 模型输出思考内容。
3. 联网搜索。
4. 文件上传后问答。
5. 长文档问答。
6. 长代码输出。
7. ECharts 输出。
8. 中途停止。
9. 重新生成。
10. 切换会话再回来。
11. 刷新页面恢复。
12. compare 多模型对比。
13. provider 限流/报错。
14. DB polling 延迟或返回旧内容。

---

## 8. 当前建议执行顺序

由于 1、3 以及 4/5 的一部分已经落地，后续顺序应调整为：

```text
1. MarkdownRenderer：AMC 风格 lazy/basic/math split + 长代码折叠 + ECharts lazy
2. MessageList：继续拆 AssistantBubble/UserBubble/Actions/Sources/Files/Error/Compare
3. messageStatus：把所有运行态徽标统一接入 deriveMessageStatuses
4. streaming.ts：补 runtime cleanup/listener/phase 回归矩阵，而不是再大改 API
5. scroll/input UX：补浏览器级滚动意图、选中文本回复、队列/离线提示等体验优化
6. useChat：最后再拆 task stream / polling / finalizer / reducer
```

短期下一轮优先做 1-3：它们直接影响用户能感知的前端体验，而且不需要推倒现有 chat 主链路。

---

## 9. 结合 AMC-WebUI 与 Hermes WebUI 的下一阶段前端体验优化路径

### 9.1 从 AMC-WebUI 借鉴：把 Markdown 能力拆成“基础渲染 + 按需重型渲染”

AMC-WebUI 的相关结构：

```text
AMC-WebUI/src/components/message/content/MessageText.tsx
AMC-WebUI/src/components/message/LazyMarkdownRenderer.tsx
```

可借鉴点：

1. `MessageText` 同时使用 `useMessageStream`、`useSmoothStreaming` 和 `LazyMarkdownRenderer`，把流式文本、平滑展示、Markdown 渲染放在清晰边界里。
2. `LazyMarkdownRenderer` 会先判断 `containsTexMathMarkdown(content)`，只有内容真的包含数学语法时才加载 Math renderer。
3. 基础 Markdown 和 Math Markdown 分离，避免普通回答也加载 KaTeX/数学插件。
4. 用户长消息有 collapse 机制，避免超长 prompt 把聊天页面顶得很长。
5. grounded response / sources 与普通 Markdown 渲染分支分离，降低消息主体复杂度。

AI Space 下一步对应落地：

```text
frontend/components/chat/markdown/
├── BasicMarkdownRenderer.tsx
├── MathMarkdownRenderer.tsx
├── LazyMarkdownRenderer.tsx
├── CodeBlock.tsx
├── LazyEChartsBlock.tsx
└── markdownComponents.tsx
```

优先级：

1. 先拆 `BasicMarkdownRenderer` / `MathMarkdownRenderer`，非数学内容不加载数学链路。
2. 再拆 `CodeBlock`，补长代码折叠和 no nested `<pre>` 浏览器断言。
3. ECharts/Mermaid/Graphviz 只在 completed 或 idle/visible 后初始化。
4. 用户长消息加 collapse，尤其是长文档粘贴、长 prompt、文件摘要场景。

### 9.2 从 Hermes WebUI 借鉴：补“聊天作为工作台”的细节体验

Hermes WebUI 不是 React/Next 架构，但它的原生 JS chat 前端有一些成熟交互值得借鉴：

相关文件：

```text
oracle-hermes/webui/static/messages.js
oracle-hermes/webui/static/ui.js
```

可借鉴点：

1. **选中文本回复**
   - Hermes WebUI 支持在聊天区域选择文本后出现“Reply with selection”浮层按钮。
   - 点击后把选中文本作为 Markdown quote 追加到 composer。
   - AI Space 可以做成更现代的浮动 action bar：引用、复制、追问、解释、生成图片/代码片段等。

2. **离线/网络恢复提示**
   - Hermes WebUI 有 offline banner、health probe、自动恢复检测。
   - AI Space 可以把 chat 请求失败分成：浏览器离线、后端不可达、provider 限流、模型能力不支持、第三方搜索失败，而不是统一 toast。

3. **发送队列与并发保护**
   - Hermes WebUI 有 `_sendInProgress`、session queue、queue drain 等机制，防止重复发送和会话切换时错投。
   - AI Space 后续可支持“当前回答生成中时继续输入下一条，排队发送”，并且在 UI 上显示队列状态。

4. **会话完成后的未读/已读管理**
   - Hermes WebUI 会区分当前正在看的会话和后台完成的会话。
   - AI Space 已有任务通知偏好，后续可进一步把 sidebar 会话未读、后台完成提示、当前页抑制策略统一。

5. **active stream/session guard**
   - Hermes WebUI 在切换会话时避免旧 stream 更新当前 pane。
   - AI Space 的 task stream / polling / restore 也应继续强化 stale event guard，尤其是 compare、多任务、后台复杂任务。

### 9.3 AI Space 下一阶段体验路线图

建议按“用户可感知收益 + 风险可控”排序：

#### A. 消息阅读体验

- 长用户消息折叠/展开。
- 长代码块折叠、复制、语言标签、diff 高亮保持。
- sources/引用卡片从正文中拆出来，避免挤压阅读。
- reasoning 默认折叠策略可配置：短 reasoning 展开，长 reasoning 折叠。

#### B. 流式期间的稳定感

- 手动上滑后不再被新 token 拉回底部。
- 只有用户在底部时才 auto-follow。
- 流式时输入框、模型选择器、侧边栏不重绘不抖动。
- search/tool/reasoning/generating/finalizing 状态顺序固定，不出现互斥状态同时显示。

#### C. Composer 工作台体验

- 选中文本后浮动操作：引用追问、复制、解释、转为 prompt。
- 生成中允许继续输入下一条，并以队列形式显示。
- 附件/搜索/reasoning/model 选择在 composer 内统一显示为 chips，避免状态隐藏。
- 对不支持 native search 的模型显示“将使用第三方搜索”，而不是“建议换模型”。

#### D. 错误与恢复体验

- 区分 provider 限流、第三方搜索失败、浏览器离线、后端不可达、用户取消。
- 对 retryable 错误提供原位重试。
- 对后台任务完成提供非打扰通知；当前正查看同一任务时抑制通知。
- 刷新/切会话恢复时，优先显示 persisted final state，再恢复 active task stream。

#### E. 工程收敛

- `MessageList.tsx` 继续瘦身到列表容器。
- `useChat.ts` 暂不大拆，先提纯 task event parser、finalizer、polling arbitration helper。
- 每个阶段保留 Node regression + browser fixture + real `/api/chat` E2E。

---

## 10. 不建议第一阶段做的事

- 不建议第一阶段重写后端 stream 协议。
- 不建议第一阶段大规模改数据库消息表。
- 不建议第一阶段直接把 `useChat.ts` 全部重构。
- 不建议同时改聊天渲染、任务恢复、计费、模型选择等多个大模块。
- 不建议在流式阶段启用完整 Markdown + 代码高亮 + ECharts + 数学渲染。

---

## 11. 最终目标状态

最终实现后，聊天主链路应达到：

```text
高频 token/event -> streaming.ts runtime store
当前 message bubble 单独订阅 runtime snapshot
流式阶段轻量渲染和平滑展示
完成阶段 finalizeAssistantMessage 写回 messages state
Markdown/Code/ECharts 只在合适时机重型渲染
状态展示由 deriveMessageStatuses 统一派生
useChat 只做编排，stream/polling/finalizer 各自独立
```

这套改造的核心价值是：

1. **性能更稳**：长回答和长历史不拖慢整个页面。
2. **状态更准**：搜索、生成、停止、失败、完成不乱跳。
3. **代码更可维护**：后续 RAG 引用、任务通知、工具调用、Deep Research 都能接入同一运行态体系。
4. **风险可控**：先增强已有机制，再逐步拆分，不推倒重写。
