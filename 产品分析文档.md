# AI Pool 产品深度分析报告

> 分析日期：2026-05-16
> 代码库版本：Go/Gin 后端（~9,000 行 Go）+ Next.js 前端（~12,000 行 TS/TSX）
> 总代码量：~22,000 行核心代码（不含 node_modules、vendor、构建产物）

---

## 一、产品简介

**AI Pool 是一个企业级多模型 AI 聚合平台**，提供统一的 API 网关和全功能 Web UI，让用户在一个界面内无缝使用 OpenAI、Anthropic、Google Gemini、DeepSeek、Kimi 等主流大模型。产品核心定位是**「AI 能力超市」**——聚合多模型、多模态能力于一体，通过积分体系进行配额管理，支持 AI 聊天、图像生成/编辑、PPT 大纲生成、技能角色扮演等多种使用场景。

### 核心数据

| 维度 | 数据 |
|------|------|
| 后端语言 | Go（~9,000 行） |
| 前端框架 | Next.js 14（~9,600 行 TSX + ~2,400 行 TS） |
| 路由框架 | Gin |
| 数据库 | GORM（SQLite/PostgreSQL 兼容） |
| 认证方式 | JWT Token |
| 前端构建 | `next build && next export` → 纯静态 dist |
| 部署模式 | 后端 Go 二进制 + 前端 Nginx/Python http.server 静态服务 |
| 支持模型 | OpenAI GPT-5.x / GPT Image-2、Claude 3.5、Gemini 2.0 Flash、DeepSeek V4/R1/V4 Flash、Kimi K2.5/K2.6 |

---

## 二、完整功能清单

### 2.1 前端功能模块

| 模块 | 页面 | 功能描述 |
|------|------|----------|
| 🗣️ **AI 聊天** | `/chat` | SSE 流式对话、多模型切换、对话历史管理、消息重新生成、消息删除 |
| 🔄 **并列对比** | `/chat`（对比模式） | 同时选中 2 个模型，相同问题对比输出，可分栏查看差异 |
| 🖼️ **图片生成** | `/image` | 文生图（gpt-image-2）、多纵横比（1:1 ~ 21:9）、分辨率（1K~4K）、质量选择（Low/Med/High/Auto）、模型选择、历史管理 |
| ✂️ **图片编辑** | `/image/edit` | 背景移除、背景替换、文字移除、画质提升、参考图上传引导 |
| 🎨 **技能系统** | `/skills`、`/skills/chat` | 预置 9 种角色技能（CEO 策略师、代码审查、创意写作、数据分析、邮件起草、会议纪要、提示工程师、论文助手、翻译）、自定义技能创建 |
| 📊 **PPT 生成** | `/ppt` | AI 生成 PPT 大纲、多种模板风格（现代简约/商务正式/创意活力/极简纯净）、页数选择（5/8/10/15）、Markdown 导出、大纲复制、文件导入 |
| 📝 **回答模板** | `/templates` | 创建/编辑/删除回答模板（system prompt 预设），用于并列对比时统一控制输出风格 |
| 🔗 **分享** | `/share/[slug]` | 对话分享链接、对比记录分享、公开访问 |
| 💳 **定价页面** | `/pricing` | 4 档积分套餐（Free/Basic/Plus/Ultra）、基础/高级/精英三级积分 |
| 🔐 **认证** | `/login`、`/register` | 登录、注册、JWT 认证 |
| 🎨 **主题系统** | ThemeProvider | CSS 变量驱动的日/夜间主题切换、完整主题变量体系 |
| 📱 **响应式侧边栏** | AppSidebar | 对话历史时间分组（今天/昨天/七天内/30天/月份）、置顶、重命名、右键菜单、折叠模式、Tooltip |
| 💬 **积分提示** | CreditBar / CreditTooltip | 实时剩余积分显示、模型消耗提示 |
| ✅ **模态控件** | ConfirmDialog / InputDialog / ShareDialog | 确认删除、重命名输入、分享对话框 |

### 2.2 后端 API 端点

| 路由 | 认证 | 功能 |
|------|------|------|
| `GET /api/models` | 否 | 全部模型列表 |
| `GET /api/models/chat` | 否 | 聊天模型列表 |
| `GET /api/models/image` | 否 | 图片模型列表 |
| `POST /api/auth/register` | 否 | 用户注册 |
| `POST /api/auth/login` | 否 | 用户登录 |
| `GET /api/users/count` | 否 | 用户总数统计 |
| `GET /api/skills` | 否 | 技能列表 |
| `GET /api/skills/:key` | 否 | 技能详情 |
| `POST /api/skills/detect` | 否 | 技能检测（根据输入自动匹配技能） |
| `POST /api/chat` | 否 | 聊天 API（SSE 流式） |
| `POST /api/files/upload` | 否 | 文件上传与内容解析 |
| `GET /api/models/tiers` | 否 | 模型等级信息 |
| `GET /api/plans` | 否 | 公开定价方案 |
| `GET /api/files/:id` | 否 | 文件详情 |
| `GET /api/files/:id/download` | 否 | 文件下载 |
| `GET /api/images/file/:filename` | 否 | 图片直接访问 |
| `GET /api/share/:slug` | 否 | 公开分享访问 |
| `GET /api/compare/share/:slug` | 否 | 公开对比记录访问 |
| `GET /conversations` | 是 | 对话列表 |
| `POST /conversations` | 是 | 创建对话 |
| `GET /conversations/:id` | 是 | 对话详情 |
| `PUT /conversations/:id` | 是 | 更新对话（置顶/重命名） |
| `DELETE /conversations/:id` | 是 | 删除对话 |
| `GET /conversations/:id/messages` | 是 | 获取对话消息 |
| `POST /conversations/:id/messages` | 是 | 添加消息 |
| `POST /images/generate` | 是 | 文生图 |
| `POST /images/edit` | 是 | 图片编辑 |
| `GET /images` | 是 | 图片列表 |
| `GET /images/:id` | 是 | 图片详情 |
| `DELETE /images/:id` | 是 | 删除图片 |
| `POST /ppt/generate` | 是 | 生成 PPT 大纲 |
| `GET /ppt/templates` | 是 | PPT 模板列表 |
| `GET /ppt` | 是 | PPT 历史列表 |
| `GET /ppt/:id` | 是 | PPT 详情 |
| `DELETE /ppt/:id` | 是 | 删除 PPT |
| `POST /conversations/:id/share` | 是 | 创建分享链接 |
| `POST /compare/record` | 是 | 保存对比记录 |
| `GET /compare/records` | 是 | 对比记录列表 |
| `DELETE /compare/record/:id` | 是 | 删除对比记录 |
| `GET /templates` | 是 | 回答模板列表 |
| `POST /templates` | 是 | 创建回答模板 |
| `PUT /templates/:id` | 是 | 更新回答模板 |
| `DELETE /templates/:id` | 是 | 删除回答模板 |
| `POST /chat/compare` | 是 | 对比问答 |
| `POST /skills/custom` | 是 | 创建自定义技能 |
| `PUT /skills/custom/:key` | 是 | 更新自定义技能 |
| `DELETE /skills/custom/:key` | 是 | 删除自定义技能 |
| `GET /user/credits` | 是 | 用户积分余额 |
| `POST /user/credits/deduct` | 是 | 扣除积分 |
| `GET /files` | 是 | 文件列表 |
| `DELETE /files/:id` | 是 | 删除文件 |

### 2.3 后端服务层结构

| 服务模块 | 文件 | 职责 |
|---------|------|------|
| `services.AIService` | `ai_service.go` | 多模型 AI 引擎调用（OpenAI/Claude/Gemini/DeepSeek/Kimi） |
| `services.ImageService` | `image_service.go` | 图片生成与编辑服务 |
| `services.PPTService` | `ppt_service.go` | PPT 大纲生成 |
| `services.SearchService` | `search_service.go` | 联网搜索 |
| `services.FileService` | `file_service.go` | 文件上传、解析、管理 |
| `services.FileParser` | `file_parser.go` | 文件内容解析器 |
| `services.AuthService` | `auth_service.go` | 认证服务 |
| `services.CreditService` | `credit_service.go` | 积分管理 |
| `skills.Loader` | `loader.go` | 技能系统加载器 |
| `embedding.Provider` | `provider.go` | 向量嵌入（可选，降级到关键词搜索） |

---

## 三、产品竞争优势

### 3.1 多模型聚合 —— 核心差异化

**业界痛点**：用户需要注册多个 AI 平台的账号（ChatGPT、Claude、Gemini、DeepSeek...），在不同的 Web UI 之间切换，管理多份账单。

**AI Pool 解法**：**统一网关 + 统一计费**。

- 一个登录、一个界面，调用所有主流大模型
- 积分体系统一消耗，无需管理多个订阅
- 同一对话上下文可自由切换模型（对比查看差异）
- 后端统一错误处理和重试逻辑，穿透不同 API 差异

### 3.2 并列对比 —— 独特价值

**「同时选 2 个模型，同一个问题，两个答案并列展示」**——这是 AI Pool 最具区分度的功能。

- 研发人员对比 GPT-5.5 和 DeepSeek R1 的回答质量
- 产品经理对比 Claude 和 Gemini 的文案风格
- 教师对比不同模型对同一知识点的解释方式
- 搭配**回答模板**（system prompt 预设）确保对比条件公平

### 3.3 技能系统 —— 场景化 AI

无需 Prompt Engineering 知识，用户通过选择预设技能即可获得定向 AI 能力：

- **CEO 策略师**：商业战略咨询
- **代码审查**：代码 Review
- **创意写作**：文案、小说、剧本
- **数据分析**：表格、CSV 分析
- **邮件起草**：商务邮件
- **会议纪要**：会议记录结构化
- **提示工程师**：Prompt 优化
- **论文助手**：学术写作
- **翻译**：多语翻译

**更关键**：用户可创建**自定义技能**，私有化定制专属 AI 角色。

### 3.4 多模态能力闭环

单一产品的 AI 能力是割裂的——ChatGPT 能聊不能精准编辑图片，专用工具能编辑但无推理能力。

AI Pool 在一个平台内提供：

| 模态 | 能力 | 技术路径 |
|------|------|----------|
| **文本** | 多模型对话、SSE 流式、上下文联贯 | OpenAI/Claude/Gemini/DeepSeek/Kimi 统一抽象层 |
| **图像生成** | 文生图、多纵横比、多种分辨率 | GPT Image-2 API |
| **图像编辑** | 背景移除、背景替换、文字移除、画质提升 | GPT Image-2 Edits API |
| **PPT** | AI 生成大纲、多风格模板、Markdown 导出 | AI 服务 + 模板引擎 |
| **文件** | 文件上传解析（文本/文档提取） | FileParser + Embedding |
| **搜索** | 联网搜索增强 | SearchService |

### 3.5 积分体系（B 端友好的计费模型）

- 三级积分（基础/高级/精英），映射模型消耗权重
- 免费版每日 30 基础积分，降低进入门槛
- Plus/Ultra 分层定价，覆盖从个人到重度用户的全部场景
- 积分管理 API 完整，可为白标客户提供自定义定价

### 3.6 Go 技术栈优势

- **性能**：相比 Python（FastAPI/Flask），Go 原生编译二进制，内存占用低，并发处理强
- **部署**：单文件二进制部署，无 Python 环境依赖，无虚拟环境
- **启动速度**：毫秒级冷启动，适合容器化弹性伸缩
- **并发模型**：Goroutine 天然适合 SSE 流式场景（每个用户连接独立 goroutine）
- **跨平台**：`GOOS=linux/windows/darwin` 一键交叉编译

---

## 四、定制化能力矩阵

### 维度一：界面定制化（UI 换肤/白标）

AI Pool 的前端架构天然支持深度界面定制，技术可行性极高。

#### 现有基础

| 能力 | 当前状态 | 定制化空间 |
|------|---------|-----------|
| **CSS 变量主题系统** | ✅ 已完成 | `--brand`, `--background`, `--surface`, `--text-primary` 等 30+ 变量覆盖全部 UI 元素色系 |
| **日/夜间主题** | ✅ 已完成 | ThemeProvider + ThemeToggle，主题切换机制完整 |
| **Logo/品牌名** | ⚡ 可定制 | 当前硬编码「AI Space」，可外化到配置项 |
| **侧边栏 icon 体系** | ✅ 已完成 | SVG icon 组件 + 自定义颜色映射 |
| **着陆页组件** | ✅ 已完成 | HeroSection / FeaturesSection / ModelsSection / StatsSection / CTASection 均可独立替换 |
| **移动端适配** | ✅ 已完成 | MobileNav 组件、响应式 CSS |

#### 可提供的定制化服务（能力边界）

```
┌───────────────────────────────────────────────┐
│              界面定制化服务                       │
├───────────────┬───────────────────────────────┤
│  一级白标      │  二级品牌化                      │
│  （3-5天）     │  （1-2周）                       │
├───────────────┼───────────────────────────────┤
│ • Logo 替换   │ • 完整品牌色系定制（30+ CSS 变量） │
│ • 品牌色调整  │ • 自定义着陆页（产品描述/特性文案） │
│ • 产品名称更改 │ • 定价页面定制（套餐/价格/货币）    │
│ • Favicon     │ • 自定义域名 + SSL               │
│               │ • 移除「Powered by」标识          │
└───────────────┴───────────────────────────────┘
```

#### 定制化改造路径

```typescript
// 当前前端/app/layout.tsx
// 可改为从 /api/whitelabel 接口动态加载样式变量

// 方式一：编译时注入（推荐）
// next build 时读取 JSON 配置 → 注入 CSS 变量
// 适合一次性交付的定制版本

// 方式二：运行时动态加载（SaaS 多租户）
// 用户登录 → API 返回 brand config → ThemeProvider 动态注入
// 适合代理运营模式
```

### 维度二：后端能力定制化

AI Pool 的后端架构以**服务层抽象**为核心，支持丰富的定制。

#### 能力定制化清单

| 定制项目 | 技术可行性 | 实现方式 |
|---------|-----------|---------|
| **自定义模型接入** | ⭐⭐⭐⭐⭐ | AIService 抽象层新增 Provider，只需实现 `Chat()` 和 `ChatStream()` 接口 |
| **自定义积分定价** | ⭐⭐⭐⭐⭐ | `config.Config` 中定义 tiers/plans，API 自动生效 |
| **私有模型部署** | ⭐⭐⭐⭐ | 替换 endpoint 为私有推理端点（vLLM/TGI），开箱支持 OpenAI 兼容协议 |
| **自定义技能库** | ⭐⭐⭐⭐⭐ | skills 目录编写 JSON 定义 + Prompt，零代码扩展 |
| **多语言界面** | ⭐⭐⭐⭐ | Next.js i18n 框架集成，文案集中管理 |
| **OAuth/SSO 集成** | ⭐⭐⭐⭐ | AuthMiddleware 适配，支持 LDAP/OIDC/OAuth2 |
| **数据持久化替换** | ⭐⭐⭐⭐⭐ | GORM 抽象层，从 SQLite 切换到 PostgreSQL/MySQL 零侵入 |
| **自有存储对接** | ⭐⭐⭐⭐ | 图片/文件存储可切换到 S3/MinIO/阿里云 OSS |
| **API Rate Limit 定制** | ⭐⭐⭐⭐⭐ | RateLimitMiddleware 配置化（IP/用户/模型级别） |
| **Webhook/回调** | ⭐⭐⭐⭐ | 异步生成任务完成后通知回调 URL |
| **日志审计** | ⭐⭐⭐⭐ | 可插入审计日志中间件，记录所有 API 调用 |
| **用量统计面板** | ⭐⭐⭐⭐ | 已有用户/对话/图片 CRUD，可扩展管理面板 |

#### 关键定制点示例

**1. 自定义模型接入（最小改动的扩展）**

```go
// internal/services/ai_service.go
// 新增自定义提供商只需：
type CustomProvider struct {
    cfg *config.Config
}
// 实现接口：
func (p *CustomProvider) Chat(ctx context.Context, req *ChatRequest) (*ChatResponse, error) { ... }
func (p *CustomProvider) ChatStream(ctx context.Context, req *ChatRequest) (<-chan ChatStreamEvent, error) { ... }
```

**2. 白标多租户（架构设计）**

当前架构的 GORM 层天然支持 `tenant_id` 字段扩展。最小改造路径：

```
1. 数据库表添加 tenant_id 列
2. 中间件提取租户上下文
3. 所有查询自动过滤 tenant_id
4. 每个租户独立配置（模型列表、积分定价、主题色）
```

---

## 五、稀缺性能力规划（可做 ≠ 能做差异化）

本章节筛选出**真正稀缺**（竞品不易复制）且**高商业价值**的能力，按可行性分级。

### 5.1 立即可做（1-2 周）

| 能力 | 稀缺性 | 实现路径 |
|------|--------|---------|
| **对比分享链接** | 高 | 当前对比记录已持久化，前端渲染 `/compare/share/:slug` 页面即可 |
| **管理员后台** | 中 | 新增后台页面：用户管理、用量统计、模型开关、日志查看 |
| **API Key 自助管理** | 高 | 后端 `/api/keys` CRUD，用户生成自有 API Key 调用网关（类 OpenAI API 兼容） |
| **自定义模型 Prompt 模板** | 中 | 当前已有模板系统，扩展到聊天场景的初始 Prompt 注入 |

### 5.2 中期规划（2-4 周）

| 能力 | 稀缺性 | 商业价值 | 说明 |
|------|--------|---------|------|
| **API 兼容层（OpenAI SDK 无缝替换）** | ⭐⭐⭐⭐⭐ | 极高 | 实现 OpenAI API 兼容端点，用户可用现有 OpenAI SDK 直接调用 AI Pool 网关，零迁移成本。这是企业客户最迫切的需求 |
| **对话历史全文搜索** | ⭐⭐⭐⭐ | 高 | 已有 Embedding 基础设施，为对话消息建立向量索引 |
| **知识库 RAG** | ⭐⭐⭐⭐ | 高 | 文件上传 → 向量化 → 检索增强生成（私有知识问答） |
| **批量测试/评估** | ⭐⭐⭐⭐ | 高 | 批量预设问题 → 多模型批量推理 → 结构化对比报告 |
| **多语言界面** | ⭐⭐ | 中 | 国际化 i18n，覆盖英文/日文/韩文 |
| **Google/WeChat OAuth** | ⭐⭐ | 中 | 降低注册门槛，社交账号一键登录 |

### 5.3 长期差异化（4-8 周）

| 能力 | 稀缺性 | 商业价值 | 说明 |
|------|--------|---------|------|
| **AI 模型路由/负载均衡** | ⭐⭐⭐⭐⭐ | 极高 | 根据问题类型自动路由到最佳模型（编程→DeepSeek V4，创意→GPT-5.5，分析→Claude）。类比 AI 模型领域的「CDN 智能路由」 |
| **多模态分析（图片/文档理解）** | ⭐⭐⭐⭐ | 高 | 上传图片、PDF → 模型理解内容并回答（当前仅文件文本解析） |
| **团队协作工作区** | ⭐⭐⭐⭐ | 高 | 共享对话、协作 Prompt 编辑、团队积分池 |
| **工作流自动化（AI Pipeline）** | ⭐⭐⭐⭐⭐ | 极高 | 串联多个 AI 调用：先搜索 → 再分析 → 再总结 → 再生成报告。可视化编排面板 |
| **私有模型部署集成** | ⭐⭐⭐⭐ | 高 | 一键接入私有 vLLM/Ollama 推理服务器 |
| **企业审计与合规** | ⭐⭐⭐⭐ | 高 | 完整操作日志、数据脱敏、内容审核接口 |

### 5.4 核心稀缺能力详述

---

#### 🔑 稀缺能力 #1：AI 模型智能路由（模型 RAG）

**为什么稀缺：**

当前行业现状是「用户在界面手动选模型」。没有任何现成的聚合平台实现了**自动负载均衡与智能路由**。

**实现方案：**

```
用户输入 question
    ↓
模型路由层（轻量分类模型 / Embedding 匹配）
    ↓
┌── 编程/代码 → DeepSeek V4（性价比最高）
├── 创意/文案 → GPT-5.5（创造力最佳）
├── 分析/推理 → Claude 3.5 / Kimi K2.6（长上下文）
├── 翻译 → Gemini 2.0（多语言最稳）
└── 默认 → GPT-5.4-mini（成本最低）
    ↓
结果统一返回（用户无感知模型切换）
```

**商业价值：**
- 用户不用再纠结「选哪个模型」
- 降低平均推理成本（用最便宜的模型完成 80% 的任务）
- 企业客户可以直接采购「能力」而非「模型」

---

#### 🔑 稀缺能力 #2：OpenAI API 兼容网关

**为什么稀缺：**

当前 AI Pool 是**自用 UI**，没有对外暴露 API。但从市场上看，ChatGPT API 兼容性是事实标准（几乎所有 LLM 工具/框架都支持 OpenAI SDK）。提供 OpenAI 兼容 API = 瞬间获得全部生态兼容。

**实现方案：**

```bash
# 用户只需要改一行代码
export OPENAI_BASE_URL="https://aipool.xxx.com/v1"
# 现有代码零改动
response = openai.chat.completions.create(model="gpt-4o", messages=[...])
# 实际背后路由到 AI Pool 的任意模型
```

**商业价值：**
- 用户现有工具链（Cursor、Copilot、LangChain、AutoGPT）全部直接可用
- 企业客户可自建 AI 中台，统一管理所有 AI 调用
- 积分/用量/审计在网关层统一控制

---

#### 🔑 稀缺能力 #3：AI 工作流（Pipeline 编排）

**为什么稀缺：**

行业现状是单一调用（一问一答）。真正的生产力来自**多步骤 AI 协奏**：

```
搜索资料 → AI 阅读总结 → 生成报告大纲 → AI 撰写初稿 → 翻译成多语言
```

**实现方案：**
- 可视化拖拽编排面板
- 串联 AI 调用链（步骤间传递上下文）
- 保存为「技能模板」，一键复用

**商业价值：**
- 从「对话工具」升级为「生产力引擎」
- 企业可将 SOP 固化为 AI 工作流
- 大幅提升单用户产出效率

---

## 六、竞品对标分析

| 维度 | AI Pool | Poe (Quora) | ChatHub | Typical API Proxy |
|------|---------|-------------|---------|-------------------|
| **多模型聚合** | ✅ 5+ 模型家族 | ✅ 收费 | ✅ 免费有限 | ❌ 仅 API 无 UI |
| **并列对比** | ✅ 核心功能 | ❌ | ✅ | ❌ |
| **图片生成** | ✅ 高质量 | ✅ | ✅ | ❌ |
| **图片编辑** | ✅ 4 种模式 | ❌ | ❌ | ❌ |
| **PPT 生成** | ✅ 专有 | ❌ | ❌ | ❌ |
| **技能系统** | ✅ 9+ 自定义 | ❌ | ❌ | ❌ |
| **联网搜索** | ✅ 内置 | ✅ | ❌ | ❌ |
| **文件解析** | ✅ 多格式 | ✅ | ❌ | ❌ |
| **回答模板** | ✅ 核心功能 | ❌ | ❌ | ❌ |
| **分享链接** | ✅ | ✅ | ❌ | ❌ |
| **积分体系** | ✅ 三级分层 | ✅ 订阅制 | ❌ | ❌ |
| **Go 性能** | ✅ 原生编译 | ❌ Python | ❌ JS | 多样 |
| **白标部署** | ✅ 可定制 | ❌ | ❌ | ✅ |
| **OpenAI API 兼容** | ❌ 规划中 | ❌ | ❌ | ✅ |

**差异化结论：** AI Pool 是市面上唯一同时具备 **多模型聚合 + 并列对比 + 图片编辑 + PPT 生成 + 技能系统 + 白标定制** 的产品。每个单一维度都不缺竞品，但**组合在一起**是稀缺的。

---

## 七、技术架构特色

### 7.1 后端架构层次

```
┌──────────────────────────────────────┐
│           Gin HTTP Router            │
├──────────────────────────────────────┤
│  AuthMiddleware │ RateLimit │ CORS   │
├──────────────────────────────────────┤
│   API Handlers (chat/image/ppt/...)  │
├──────────────────────────────────────┤
│   Services (AI/Image/PPT/File/...)   │
├──────────────────────────────────────┤
│   Provider Abstraction Layer         │
│   (OpenAI/Claude/Gemini/DeepSeek/Kimi)│
├──────────────────────────────────────┤
│   GORM DB │ Config │ Embedding       │
└──────────────────────────────────────┘
```

### 7.2 流式架构

```
用户请求 → Gin Handler → Select Provider
  ↓
创建 goroutine → 调用 Provider.ChatStream()
  ↓
SSE Write: data: {"content": "..."}
  ↓
前端 EventSource → 逐块追加到 MessageList
```

- 每个长连接独立 goroutine
- 前端支持 AbortController 中断生成
- 后端通过 context.Context 传递取消信号

### 7.3 静态导出架构

```
next build && next export
    ↓
/dist (纯静态 HTML/CSS/JS)
    ↓
Python http.server / Nginx 静态服务
    ↓
API 请求代理到 Go 后端（proxy_pass / /api/*）
```

前端 SSR 降级为纯静态，意味着：
- 部署极简（Go 二进制 + 静态文件目录）
- 零 Node.js 运行时依赖
- CDN 友好（全站静态可缓存）

---

## 八、总结与建议

### 产品定位

**AI Pool = 企业级多模型 AI 能力中台**

不是另一个 AI 聊天工具，而是企业构建自有 AI 能力的**基础设施层**。

### 核心卖点（一句话）

> **「一个平台调用所有大模型，还能定制成你的品牌」**

### 建议优先推进

1. **OpenAI API 兼容层** — 最快打开企业市场的入口（2 周）
2. **白标部署文档 + 配置模板** — 让客户 30 分钟完成品牌化部署（1 周）
3. **智能模型路由** — 提升用户体验的同时降低成本（3 周）
4. **管理员后台** — 让客户能独立运营（2 周）

### 不适合做什么

- ❌ 重客户端（Electron/移动端原生）— 端内资源应投入 Web 体验优化
- ❌ 自建模型训练 — 应该专注网关能力，模型靠集成
- ❌ 复杂的多人实时协作 — 简单分享和团队积分池足够

---

> **本报告基于 /workspace/aipool 代码库深度分析生成**
